# Public browser configuration only. Never put AI keys or database credentials here.
VITE_API_BASE_URL=http://localhost:8080
# Vite development proxy target (not included in the browser bundle).
API_PROXY_TARGET=
 
